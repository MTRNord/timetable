{
  "name": "timetable",
  "description": "A timetable App with json and XML API for pupils from pupils",
  "repository": {
    "type": "git",
    "url": "https://github.com/MTRNord/timetable"
  },
  "scripts": {
    "preinstall": "bash deps.sh",
    "start": "node app.js && service php-fpm start && service nginx start && service mysql start && service nginx start"
  },
  "version": "0.0.1",
  "main": "app.js",
  "engines": {
      "node": "0.10.22"
  }
}